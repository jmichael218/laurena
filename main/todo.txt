0: X rule charset templaté avec algo (readwhile, readuntil, skipwhile, skipuntil)

1:
   X stream out dans le context pour l'affichage de la sortie
   X context->count(s) => source location + stream out.

2:
   X rules symbols
		X implemented for KEY = chartype
		X implemented for KEY = basic_string<chartype>

4:
    X rules integer<int>

5:

	X dégager grammaire de L

6: 
	X loader<chartype> dans L0

7: 
	X tabs<chartype> dans LO
	NO utiliser std::fill ou std::space pour insérer les espaces

8:
    X deplacement des const charset en l0
	* reduction des toolsets sauf bitset
		X cstring, 
		X stdstring, 
		filename, 
		X stdstream
	X virer source_location
	* faire tests dans test0 pour algos avec const char* const char[] std::string std::istream std::wstring

9:
   X deplacer basetype
   X virer / deplacer chartype
   X deplacer voidptr
   X rule keyword
   * parsing avec des rules de tabulations
   * rule OR (multi choix)
      X avec unicité du is_candidate
	  * possible avec multiplicité du is_candidate ?




10:
	* reduction des constants dans L
	* refonte de annotation format 

20:
	* string array en symbols <int,string> ou <string,string>.
	* enrichir symbols avec les fonctions de string array
